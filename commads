# for istallation of python packages

$ python3 -m venv venv
$ ls
$ source venv/bin/activate
# try also if above not run  $ venv/Scripts/activate

# now go to in /home/fastapitodo

$ mkdir app
$ cd app
$ git clone https://github.com/RohitPatil18/docker-fastapi-test.git
 
# now do ls and see all the direcory & files coppied from git 

$ cd ..
# output >>>> app  venv
# go to /home/fastapitodo and

$ code .

$ pip install -r requirements.txt

# then go to /home/fastapitodo and after run this bellow coomand the server will be restarted for API like //127.0.0.1:8000 (press CTRL+C to quite ) 

$ uvicorn app.main:app

# after above we will go to in file database.py and set own password and again rrun bellow command
 
$ uvicorn app.main:app

# now type http://localhost:8000 in the browser the output will show of like id, name description, date .
# after that type http://localhost:8000/docs in the browser and output will show of fastAPI to do


  $ uvicorn app.main:app --reload

